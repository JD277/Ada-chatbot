<Mensaje>:
    size_hint:1,None
    size:root.size
    texto_respuesta: res
    texto_pregunta: pre
    GridLayout:
        cols:1
        size_hint: 1,None
        height: self.minimum_height
        Label: # Pregunta
            id: pre
            padding: 10
            text: 'Para hacer que el rectángulo de fondo ocupe el mismo tamaño que el texto en el Label, necesitas ajustar la propiedad size_hint_x del Label a None y calcular el ancho dinámico del texto para que el rectángulo se ajuste automáticamente.'        
            size_hint: None,None
            width: root.size[0] - 30
            text_size:(root.size[0] - 30, None)
            valign: 'middle'
            bold: True
            font_size: 18
            height: self.texture_size[1] + 20
            canvas.before:
                Color:
                    rgba: (0.2, 0.6, 0.8, 0.5)  # Color en formato RGBA
                Rectangle:
                    pos: self.pos
                    size: root.width, self.height
        Label: # Respuesta
            id: res
            padding: 10
            text: 'Para hacer que el rectángulo de fondo ocupe el mismo tamaño que el texto en el Label, necesitas ajustar la propiedad size_hint_x del Label a None y calcular el ancho dinámico del texto para que el rectángulo se ajuste automáticamente.'        
            size_hint: None,None
            width: root.size[0] - 30
            text_size:(root.size[0] - 30, None)
            valign: 'middle'
            bold: True
            font_size: 18
            height: self.texture_size[1] + 20
            canvas.before:
                Color:
                    rgba: (0.2, 0.2, 0.8, 0.5)  # Color en formato RGBA
                Rectangle:
                    pos: self.pos
                    size: root.width, self.height 
    

<Interface>:
    prompt_input: input
    carousel: scroll
    send: btn_send
    canvas: 
        Color: 
            rgba:48/255, 55/255, 66/255,1
        Rectangle:
            size: root.size
            pos: self.pos


    ScrollView:
        size_hint: None,None
        size: root.size  
        do_scroll_x: False
        do_scroll_y: True   
        padding:50
        GridLayout:
            id: scroll
            cols: 1
            size_hint_x: 1
            size_hint_y: None  
            height: self.minimum_height  
            padding: 10
            spacing: root.space
                       

    GridLayout:
        width: root.width
        cols: 1
        TextInput:
            id: input
            text: 'Ask something...'
            font_size: 20
            background_color: 179/255, 199/255, 232/255,1
            foreground_color: 30/255, 34/255, 41/255,1
        GridLayout:
            cols: 2
            height: 200
            Button:
                text: 'Record'
                background_normal: ''
                background_down: ''
                background_color: 28/255, 58/255, 107/255,1   
            Button:
                id: btn_send
                text: 'Send'
                background_normal: ''
                background_down: ''
                background_color: 50/255, 88/255, 148/255,1
                on_release: root.ask()

                
            
        


